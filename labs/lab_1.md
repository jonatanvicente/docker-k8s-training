

## Running our first container

- Download the images `hello-world` and `nginx` and run them.

```
docker pull hello-world:latest
docker pull nginx
```

## Inspecting containers

- Use `docker logs` and `docker inspect` to view container state, configuration, and events.